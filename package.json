{
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name ecura-landing",
    "deploy:ping": "wrangler pages deploy public --project-name ecura-landing && node ping-indexnow.js",
    "ping": "node ping-indexnow.js"
  },
  "dependencies": {
    "wrangler": "^4.119.0"
  }
}
